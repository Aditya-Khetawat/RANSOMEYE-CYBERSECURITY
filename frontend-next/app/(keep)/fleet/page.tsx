import { FleetView } from "@/entities/ransomeye/ui/FleetView";

export const metadata = {
  title: "RansomEye | Endpoint Fleet",
  description: "Continuous ransomware risk across protected endpoints, and real domain-isolation state.",
};

export default function FleetPage() {
  return <FleetView />;
}
