import SettingsPage from "./page.client";

export default function Page() {
  return <SettingsPage />;
}

export const metadata = {
  title: "Settings | RansomEye",
  description: "Real system status - what's actually running, not a settings form.",
};
