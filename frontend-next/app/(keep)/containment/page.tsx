import { ContainmentView } from "@/entities/ransomeye/ui/ContainmentView";

export const metadata = {
  title: "RansomEye | Containment",
  description: "Stop ransomware propagation while preserving forensic context — actions, policy, and playbooks.",
};

export default function ContainmentPage() {
  return <ContainmentView />;
}
